!(function($){
	"use strict";

	jQuery(document).ready(function($) {

	});

	jQuery(window).on('resize', function() {
    DenaliSubmenuAuto();
	});

	jQuery(window).on('scroll', function() {

	});

})(jQuery);
